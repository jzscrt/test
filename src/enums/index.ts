export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  DISABLED = 'disabled',
  REVIEW = 'for review',
}

export enum UserRole {
  SU = 'superadmin',
  ADMIN = 'admin',
  USER = 'user',
}
